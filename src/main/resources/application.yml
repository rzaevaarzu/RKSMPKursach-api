---
server:
  server.port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/KursachRA
    username: postgres
    password: Lyambda_762221
  jpa:
    hibernate:
      ddl-auto: update
